# you-need

## Running the app on command line
(in main directory)
```
mvn -pl you-need-backend spring-boot:run
```

## Dockerizing the app
Run in main directory
```
mvn clean install
cd you-need-docker
docker compose up
```
